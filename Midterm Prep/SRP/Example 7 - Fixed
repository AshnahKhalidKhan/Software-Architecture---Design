class Logger {
  logError(message) {
  // Logging error messages
  }

  logInfo(message) {
  // Logging informational messages
  }
}

class EmailService {
  sendEmail(message) {
   // Email sending logic
  }
}

class ErrorEmailNotifier {
  sendNotification(message) {
  // Logic to notify on critical errors, possibly using EmailService
  }
}