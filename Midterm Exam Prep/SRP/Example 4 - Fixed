class Circle {
  constructor() {
    this.radius = 0;
  }

  setRadius(radius) {
    this.radius = radius;
  }
  
  calculateArea() {
    // Calculation logic for circles
  }
}

class Square {
  constructor() {
    this.width = 0;
  }
  
  setWidth(width) {
    this.width = width;
  }
  
  calculateArea() {
    // Calculation logic for squares
  }
}