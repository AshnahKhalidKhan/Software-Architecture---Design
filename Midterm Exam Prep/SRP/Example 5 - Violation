class User {
  constructor() {
    this.username = '';
    this.password = '';
  }

  setCredentials(username, password) {
    this.username = username;
    this.password = password;
  }
  
  login() {
    // Authentication logic
  }
  
  sendEmail() {
    // Email sending logic
  }
}